# آدرس بک‌اند (api-gateway). در توسعه از طریق پروکسی Vite استفاده می‌شود.
VITE_API_TARGET=http://localhost:8090

# اگر می‌خواهید مستقیماً (بدون پروکسی) به یک baseURL وصل شوید، این را ست کنید:
# VITE_API_BASE=http://localhost:8090
